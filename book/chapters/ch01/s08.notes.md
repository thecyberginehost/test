# Continuity Notes: Ch 1, Section 8

PASS

Verified against book/bible.md (Ch 1 Sec 8 facts block, Sec 6 public-format
canon, Ch 2 timeline boundary) and book/chapters/ch01/s07.md for flow.

## Checks performed (all clean)

- **Failing card text:** Matches the canonical card recorded in bible lines
  440-454 exactly. All lines present and in order: `ASSIGNING CLASS...`,
  `> class assignment initiated [subject: KESSLER, AARON]`,
  `> evaluating candidate set... matched`, `> writing class field`,
  `!! EXCEPTION`, `!! value did not resolve`,
  `!! class field returned no valid assignment`, `> retrying...` (x2),
  `> evaluating candidate set... no match` (x2),
  `> fallback assignment unavailable`, `> class field: <unresolved>`,
  `> leaving field open`. The section stages the card across progressive reveals
  (s07 hand-off: first character holds, then scroll), but the assembled content
  is byte-for-byte the canon. No deviation.

- **Dim-comment second layer:** Matches bible lines 467-470 exactly:
  `> # subject does not parse to existing class set`,
  `> # cannot discard: subject is conscious, active, integrated`,
  `> # cannot reassign: candidate set exhausted on first pass`,
  `> # recommend` (cuts off mid-thought). Rendered dimmer/set-back like comments
  beside code, NOT addressed to Aaron, the System narrating about him. Correct.

- **CH 2 BOUNDARY (critical) - CLEAN:**
  - Class is NEVER named NULL_OPERATOR.
  - NO "flagged for review" line appears.
  - NO editable character sheet, no blinking cursor offered to Aaron (the word
    "cursor" appears once at line 72 only as a metaphor for the dim text's
    cut-off edge, not an editable UI element).
  - The raw token at line 74 flicks past "in a color he had no name for" and
    Aaron explicitly does NOT hold, parse, or recognize it as his class name
    ("before he could hold it long enough to know it was a word, let alone what
    word, let alone that it was his"). Reinforced at line 82. Acceptable per the
    boundary rule.

- **The reveal lands:** Aaron grasps he is "the exception" (lines 92, 27, 88,
  90). He sees a second layer EXISTS beneath the public card but cannot fully
  read it (lines 74-76, 82). Matches bible reveal spec.

- **Flow from s07:** Seamless. s07 ends with the header coming up wrong,
  off-baseline, writing the first character then beginning to scroll. s08 opens
  on that exact scroll ("It scrolled the way a deploy scrolls when the deploy is
  going wrong") and line 5 picks up the held first character. Continuous,
  same Tuesday, 31st floor. Lena/Dwyer/Herbalist/Skirmisher all consistent with
  s07 and bible cast.

- **Voice / register:** Dry engineer register and dread held throughout
  (crash-log framing, "working as intended" lineage, the dead-box callback at
  line 45). POV held tight on Aaron.

- **Em dashes:** None. Prose uses comma-chains, periods, and parentheses only.
  Compliant with the hard voice rule.

- **System block formatting:** Uses canonical `[ SYSTEM ]` bracket header
  (lines 7, 18) in the same flawless typeface as the welcome; dim second layer
  rendered distinctly per Sec 6 format canon. Compliant.

- **Hand-off to s09:** Lands. The `> # recommend` line cuts off mid-thought
  (lines 70-72) and Aaron leans into the gap "to read what it was saying about
  him," realizing the second layer is the System talking to itself about him
  (lines 72, 76, 96). Sets up s09 correctly.
