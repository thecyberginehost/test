# Continuity / Style Notes: Ch 1, Section 3

Target: book/chapters/ch01/s03.md
Checked against: book/bible.md (Ch 1 Sec 3 facts block, Protagonist, System rules),
book/style.md (no-em-dash rule, LENGTH/PACING), book/chapters/ch01/s02.md (flow).

## Issues

- **fix — Em dash in dim layer (line 17).** s03 reads
  `> # subject KESSLER, A. — assessment returned no valid class`.
  Bible canonical clean version uses a colon, no dash:
  `> # subject KESSLER, A.: assessment returned no valid class`.
  Replace the ` — ` with `: ` to match canon and house style.

- **fix — Em dash in dim layer (line 19).** s03 reads
  `> # discard? — denied. subject is conscious. cannot null a live account.`
  Bible canonical: `> # discard? denied. subject is conscious. cannot null a live account.`
  Remove the ` — ` (canon uses just a space after the question mark).

- **fix — Em dash in dim layer (line 20).** s03 reads
  `> # assign nearest? — rejected by subject. integrity check failed.`
  Bible canonical: `> # assign nearest? rejected by subject. integrity check failed.`
  Remove the ` — `.

  (These three are the only em dashes in the section. They violate the bible's
  canonical CLEAN dim-layer text AND the house no-em-dash rule. All other dim
  lines — 18, 21, 22, 23 — match canon verbatim. The prose body contains no em
  dashes.)

## Verified clean

- **Failed card (lines 5-7):** matches canonical verbatim:
  `[ SYSTEM ] / CLASS: [unresolved] / status: unhandled`. PASS.
- **NULL_OPERATOR and "unhandled":** both land. The class token blinks and will
  not commit (line 25); "unhandled" appears on both layers (top card line 7, dim
  layer line 23), the System agreeing with itself. Matches bible. PASS.
- **Debug-overlay (read-only) ability established:** dim grayed-back comment
  layer surfaces under the failed card, scrolls with his eyes not his fingers,
  fixed to his sight, invisible to the room; everyone else's card is sealed
  surface-only (lines 13-16, 29, 31). PASS.
- **No editing/annotating here:** he reads only; reaches to read the next line
  but is interrupted before going deeper (line 39). Consistent with bible ("He
  did NOT edit or annotate anything"). PASS.
- **Physical toll:** pressure/heat behind the eyes crests, wet click as the
  second layer surfaces (lines 9, 13). Builds on s02 pressure. PASS.
- **Realization beat:** "unhandled" read as a shipped crash-log line; System
  crashed trying to assign a class; "decision deferred" / no handler = the window
  is open; dread mixed with dark thrill (lines 11, 39). Dry-register-meets-thrill
  delivered. PASS.
- **Flow from s02:** s02 ends "The card began to throw." s03 opens "It threw the
  way a job threw..." Seamless handoff. PASS.
- **Rift interruption hands into s04:** building coughs three floors down, first
  scream climbs the stairwell, "Aaron's read snapped shut" (lines 41-45).
  Matches bible's Sec 3 -> Sec 4 handoff. PASS.

## Length

- Word count: ~740 words. Under the ~750-word section cap. PASS (no trim needed).

## Summary

Not a PASS. Three "fix"-level em-dash issues in the dim Operator layer (lines
17, 19, 20) that diverge from the bible's canonical clean dim text and break the
house no-em-dash rule. Everything else (failed card, NULL_OPERATOR, read-only
overlay, no-edit, toll, flow, rift handoff, word count) is clean.
