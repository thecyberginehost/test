# Continuity notes: Ch 4, Section 3

Target: book/chapters/ch04/s03.md
Checked against: book/bible.md (Ch 4 Sec 3 facts; ANNOTATION RULES; decode-debt ledger),
book/style.md, prior section book/chapters/ch04/s02.md.

Verdict: NOT CLEAN. One door-logic blocker, one word-count overage. Stat/debt
math and the canonical blocks are correct.

## Issues

- **blocker (door logic not self-consistent):** The open condition as printed in
  the overlay (line 15) is `open condition: integrity <= 0 AND lock_state ==
  UNLOCKED`. Aaron cannot touch `integrity` (it stays 35, WRITE LOCKED per PATCH
  0007), so `integrity <= 0` is FALSE. Setting `lock_state` to UNLOCKED makes the
  open condition `FALSE AND TRUE` = FALSE. Likewise the gate logic (line 14)
  `hold while (integrity > 0) OR (lock_state == LOCKED)` still holds on
  `integrity > 0` (35 > 0) no matter what lock_state is. By the rules as written
  the shutter should NOT open. The section narrates it opening anyway (lines
  27-32). NOTE: this flaw originates in the bible's Ch 4 Sec 3 spec (lines
  994-996 and 1002-1003), which the section reproduces verbatim. The section is
  faithful to the source, but the source's exploit does not work as stated. To
  make the either/or actually be "the door's own mistake" (line 17), the open
  condition needs to be an OR on lock alone (e.g. open when `integrity <= 0 OR
  lock_state == UNLOCKED`, or gate `hold while (integrity > 0) AND (lock_state ==
  LOCKED)`). Flag up to the orchestrator: fix the bible spec and the overlay
  block together, since "verbatim match to bible" and "self-consistent logic"
  currently conflict.

- **fix (length over target):** Prose word count is approximately 725 words
  (excluding the `> #` overlay block and the `[ SYSTEM ]` block). Over the ~700
  guidance and pushing the ~750 section cap in style.md. Trim ~30-60 words,
  most easily from the two reflection paragraphs (lines 34 and 36), which restate
  the running-tab / literacy point twice.

## Verified clean

- **Overlay block (lines 9-15)** matches bible lines 990-996 verbatim.
- **ANNOTATION ACCEPTED block (lines 25-30)** matches bible lines 1000-1005
  verbatim.
- **Cost / tier:** Tier 0, -1 Level, Level 3 -> Level 2 (line 23 "Level 3 going
  to two"). Correct per ANNOTATION RULES (Tier 0 ~1 level) and bible line 1007.
- **decode_debt:** running tab conveyed as "Three" with the One/Two/Three ledger
  recap (line 34: conference room / fire door / here). Matches running
  decode_debt 3/100 (was 2). Running-tab beat present and on-character ("the
  level is a coin in a pocket. This was a thing accruing"). Note: section does
  not print the literal "3/100" or the HP 60 figure; both are fine (no stat block
  is shown this section, consistent with bible note "HP now 60/60").
- **Guardrail / mid-chapter Level 2:** section correctly states the drop is
  Level 3 -> 2. Sanctioned: Tier-up wave in s04 re-climbs him; this is a Tier 0
  edit, not a deep-edit trough, and recovery is immediate next section.
- **Toll:** small (thread of blood on lip, right-eye heat flares once, breath
  half-short) per bible line 1010. Correct, smaller than Ch 2.
- **Literacy-forced-deeper point** present (line 36: "It had made him read
  better... the staleness pushing him a layer down, the bill climbing while the
  trick got cleverer"). Matches bible line 1012.
- **Boundary:** ends exactly as the Tier-up wave begins (lines 38-40, public
  block counting UP, sky peeling open, Tess "That's not one rift"). Hands into
  s04 as specified. Flows cleanly from s02's hand-off ("put his sight back into
  the seam... started to read").
- **Tess** in character (sharp, reads the changed smell, calls the multi-rift a
  beat early), consistent with her Scout/kindred-reader profile.
- **Voice:** No em dashes anywhere (checked prose and both blocks; arrows are
  rendered as the word "toward" / "to", e.g. line 21, line 27). No rule-of-three
  cadence detected. Dry register intact. PATCH 0007 scope (his account only,
  adjacent vars open) honored.
